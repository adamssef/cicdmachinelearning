display:
  default:
    display_options:
      fields:
        name:
          label: Utente
        link:
          label: Operazioni
      pager:
        options:
          expose:
            items_per_page_label: 'Voci per pagina'
      exposed_form:
        options:
          submit_button: Filtra
