display:
  default:
    display_options:
      fields:
        status:
          label: Stato
        operations:
          label: Operazioni
      pager:
        options:
          expose:
            items_per_page_label: 'Voci per pagina'
      exposed_form:
        options:
          submit_button: Filtra
      filters:
        langcode:
          expose:
            label: Lingua
