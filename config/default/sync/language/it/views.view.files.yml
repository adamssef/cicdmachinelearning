display:
  default:
    display_options:
      fields:
        status:
          label: Stato
      pager:
        options:
          expose:
            items_per_page_label: 'Voci per pagina'
      exposed_form:
        options:
          submit_button: Filtra
      filters:
        status:
          expose:
            label: Stato
  page_2:
    display_options:
      pager:
        options:
          expose:
            items_per_page_label: 'Voci per pagina'
