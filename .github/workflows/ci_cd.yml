name: CI/CD for ML

on:
  push:
    branches: [ main ]

jobs:
  train-and-report:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Install CML
        run: npm install -g @dvcorg/cml

      - name: Install Python build tools and dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel build
          pip install --prefer-binary --only-binary=:all: -r requirements-ci.txt

      - name: Run training and generate report
        run: python train.py

      - name: Commit report with CML
        env:
          REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          cml send-comment reports/report.md --commit
